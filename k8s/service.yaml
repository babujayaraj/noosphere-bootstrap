apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: devops-tech-test
  labels:
    app: devops-tech-test-api
spec:
  selector:
    app: devops-tech-test-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
